worker = Proc.spawn (λ msg => * msg 2).
x = Proc.send worker 5.
result = Proc.recv worker.
# y = IO.print result.

# just prints it out
> result.

# does not terminate rn, it actually should be terminating..